# problem:

```
Could not build image[m: java.io.IOException: Cannot run program "docker-credential-desktop": 
Solution:
In ~/.docker/config.json change credsStore to credStore
```

# problem:
```
Make sure following programs are up and running locally:
- Docker
- MySQL
```
